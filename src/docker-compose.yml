version: '3'
services:
  eventstore:
    image: 'eventstore/eventstore:release-3.7.0'
    ports:
      - '1113:1113'
      - '2113:2113'
  broker:
    image: 'crossbario/crossbar:community'
    ports:
      - '8000:8000'
    volumes:
      - ./server/.crossbar:/node/.crossbar
  servers:
    build:
      dockerfile: Dockerfile
      context: ./server
    depends_on:
      - eventstore
    command: 'dotnet ./Adaptive.ReactiveTrader.Server.Launcher.dll --populate-eventstore'
  client:
    build:
      dockerfile: Dockerfile
      context: ./client
    volumes:
      - /app/node_modules
      - ./client:/app
    ports:
      - '3000:3000'
  analytics:
    build:
      dockerfile: Dockerfile
      context: ./server
    command: 'dotnet ./Adaptive.ReactiveTrader.Server.Analytics.dll config.docker.json'
    depends_on:
      - broker
      - eventstore
  pricing:
    build:
      dockerfile: Dockerfile
      context: ./server
    command: 'dotnet ./Adaptive.ReactiveTrader.Server.Pricing.dll config.docker.json'
    depends_on:
      - broker
      - eventstore
      - analytics
  referencedataread:
    build:
      dockerfile: Dockerfile
      context: ./server
    command: 'dotnet ./Adaptive.ReactiveTrader.Server.ReferenceDataRead.dll config.docker.json'
    depends_on:
      - broker
      - eventstore
      - pricing
  tradeexecution:
    build:
      dockerfile: Dockerfile
      context: ./server
    command: 'dotnet ./Adaptive.ReactiveTrader.Server.TradeExecution.dll config.docker.json'
    depends_on:
      - broker
      - eventstore
      - referencedataread
  blotter:
    build:
      dockerfile: Dockerfile
      context: ./server
    command: 'dotnet ./Adaptive.ReactiveTrader.Server.Blotter.dll config.docker.json'
    depends_on:
      - broker
      - eventstore
      - tradeexecution
